# PyMethodDef-Ghidra-Finder
A usefull script for ghidra that allows you find c++ exported functions to python (wrappers functions) through the PyMethodDef structure.
Every Help for patch that code is wellcome, the main purpose is to improve that script, for avoid fake positives.
